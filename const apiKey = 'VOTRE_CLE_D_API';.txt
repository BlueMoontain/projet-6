const apiKey = 'VOTRE_CLE_D_API';
const searchTerm = 'J.K. Rowling'; // Remplacez par l'auteur ou le titre que vous recherchez
const searchType = 'inauthor'; // 'inauthor' pour rechercher par auteur, 'intitle' pour rechercher par titre

const url = `https://www.googleapis.com/books/v1/volumes?q=${searchType}:${encodeURIComponent(searchTerm)}&key=${apiKey}`;

fetch(url)
  .then(response => response.json())
  .then(data => {
    console.log(data); // Affiche les données de la recherche dans la console
    // Vous pouvez maintenant traiter les données ici, par exemple, afficher les titres des livres par cet auteur ou avec ce titre.
  })
  .catch(error => {
    console.error('Une erreur s\'est produite:', error);
  });